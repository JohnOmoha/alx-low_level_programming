#include <stdio.h>
/**
 * main - prins the alphetic
 * Return: Always (success)
 */
/* more headers goes there */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
